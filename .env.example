# Copy to .env for local overrides. Never commit real credentials or API keys.
FRONTEND_PORT=8080
JPA_DDL_AUTO=create-drop
H2_CONSOLE_ENABLED=false
MAX_FILE_SIZE=1MB
MAX_REQUEST_SIZE=1MB
CORS_ALLOWED_ORIGINS=http://localhost
